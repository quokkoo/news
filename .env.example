# 复制为 .env 后填入自己的 key（.env 已被 .gitignore 忽略，不会提交）
#   cp .env.example .env
ANTHROPIC_API_KEY=

# 可选：覆盖默认模型
# MODEL_CURATE=claude-sonnet-5
# MODEL_BRIEF=claude-opus-5

# 可选：强制降级模式（不调用 AI，纯规则排序）
# DEGRADED=1
