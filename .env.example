# 本站默认不使用 AI，运行 npm run pipeline 不需要任何密钥。
# 这个文件只在你想启用「可选：AI 精选」（见 README 底部）时才用得到：
#   cp .env.example .env
ANTHROPIC_API_KEY=

# 可选：覆盖默认模型
# MODEL_CURATE=claude-sonnet-5
# MODEL_BRIEF=claude-opus-5

# 可选：即使配置了 ANTHROPIC_API_KEY，也强制走规则排序（不调用 AI）
# DEGRADED=1
